# Carrier facts that have NO machine-readable source in the repos themselves.
#
# Everything the repos already know — display name, domain, version, icon,
# release date, repo URL — is read from the GitHub API by scripts/generate.py.
# Do not repeat any of that here; it will be ignored and it will go stale.
#
# `scripts/generate.py` FAILS THE BUILD if the org has a carrier repo that is
# missing from this file, or if this file lists a repo that no longer exists.
# That hard failure is the whole point: it is what stops the site from
# quietly falling behind the suite when a new carrier lands.
#
# Fields per repo:
#   region     free-text coverage shown in the table ("Netherlands", "Nordics")
#   countries  ISO 3166-1 alpha-2 codes, used for the flag column. ALWAYS quote
#              them — YAML 1.1 reads a bare NO (Norway) as the boolean false,
#              and the same trap waits for ON, OFF, Y and N.
#   auth       "account" (user logs in), "trackingnr" (no account needed) or
#              "apikey" (the user brings their own official API credentials).
#              A carrier whose backends authenticate differently writes a
#              mapping instead, keyed by the SAME backend labels its own
#              const.py declares in CAPABILITIES_BY_VARIANT (a label that is
#              not in there fails the build, so the two pages can never name
#              the same backend differently):
#                  auth:
#                    Informed Delivery:
#                      kind: account
#                    API Tracking:
#                      kind: apikey
#                      input: tracking code
#              Only reach for that when the auth kinds genuinely differ —
#              one backend needing an extra field is `input` prose, not a
#              second entry.
#   input      what the user types when auth is "trackingnr". Phrase it to sit
#              mid-sentence ("tracking code", not "Tracking code") — the
#              site capitalises it where it needs a capital. With the mapping
#              form of `auth` it goes inside each backend, never next to it.
#   blurb      one sentence, sentence case, no trailing period
#   directions "incoming" or "incoming+outgoing"
#   aliases    optional list of {name, blurb, region?, countries?} — an extra
#              site/profile listing for the same repo under a second brand
#              name (own row, own blurb, same repo link/version/icon/
#              capabilities as the primary entry). Use when one repo answers
#              to two names a user would search separately (e.g. a carrier's
#              own locker/pickup brand); it is not a second integration.
#              region/countries default to the primary entry's — set them on
#              the alias only when the two brands don't cover the same
#              territory (e.g. one brand name per country within the repo's
#              overall coverage).
#   icon       optional override of the repo's own brand/icon.png, for this
#              site's primary row only — a PNG in data/icons/, 256x256 like
#              every other brand icon (tools/icon_audit.py checks it). Use it
#              only when one repo lists here under two brand names and the
#              primary row should carry one brand's mark while an alias row
#              carries the other (ha-posten-bring). HA and HACS keep reading
#              the repo's own icon; this is the listing only.
#   name       optional override of the manifest's display name, for this
#              site's primary row only. The manifest name stays the source of
#              truth everywhere else (HA UI, HACS, README) — use this only
#              when the manifest name carries scope an alias entry already
#              covers (e.g. "X, including Y" once Y has its own alias row)
#              and the shorter form reads better as a site listing.

ha-4px:
  region: Cross-border
  countries: []
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: 4PX's tracking layer for cross-border parcels shipped from China

ha-ampere:
  region: Netherlands
  countries: ["NL"]
  auth: trackingnr
  input: tracking link from the shipping e-mail
  directions: incoming
  blurb: Ampère — bol.com's own last-mile delivery brand, tracked from the link in their shipping e-mail

ha-an-post:
  region: Ireland
  countries: ["IE"]
  auth: account
  directions: incoming
  blurb: An Post — Ireland's national postal operator, with a watchlist for parcels not yet linked to your account

ha-aramex:
  region: Australia and New Zealand
  countries: ["AU", "NZ"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Aramex's keyless AU/NZ consumer tracking network

ha-better-trucks:
  region: United States
  countries: ["US"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Better Trucks — US last-mile package delivery, tracked without an account

ha-boxnow:
  region: Bulgaria, Greece, Croatia and Cyprus — pick your country's BoxNow backend at setup
  countries: ["BG", "GR", "HR", "CY"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: BoxNow locker/pickup-point parcels, tracked the same way as on the BoxNow website

ha-bpost:
  region: Belgium
  countries: ["BE"]
  auth: trackingnr
  input: tracking code + postal code
  directions: incoming
  blurb: bpost parcels, Belgium's national postal service

ha-budbee:
  region: Nordics and Benelux
  countries: ["SE", "NL", "BE", "DK", "FI", "NO"]
  auth: trackingnr
  input: tracking code or order number
  directions: incoming+outgoing
  blurb: Budbee home deliveries and Budbee Box lockers, without the e-mail address their tracking page asks for

ha-cainiao:
  region: Cross-border
  countries: []
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Alibaba's tracking layer for AliExpress, Temu, Shein and similar shops

ha-canpar:
  region: Canada
  countries: ["CA"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Canpar Express — Canadian parcel delivery and pickup-point tracking

ha-ceska-posta:
  name: Ceska Posta
  region: Czechia
  countries: ["CZ"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Czechia's national postal operator, whose pickup-point and locker network operates under the Balikovna brand
  aliases:
    - name: Balikovna
      icon: balikovna.png
      blurb: Balikovna — Ceska Posta's own pickup-point and locker network, same backend and tracking codes

ha-correos:
  region: Spain
  countries: ["ES"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Spain's national postal operator

ha-ctt:
  region: Portugal
  countries: ["PT"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Portugal's national postal operator, tracked through its public keyless tracker

ha-dao:
  region: Denmark
  countries: ["DK"]
  auth: account
  directions: incoming
  blurb: DAO — sign in once and every parcel DAO's own app or site already links to your account shows up automatically

ha-delhivery:
  region: India
  countries: ["IN"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Delhivery — India's e-commerce delivery network, tracked by tracking code, no account needed

ha-dhl:
  region: Germany and Poland
  countries: ["DE", "PL"]
  auth: account
  directions: incoming+outgoing (DE); incoming+observed (PL)
  blurb: DHL Paket (DE) and DHL Parcel Polska (PL), tracked through your own DHL account

ha-dhl-nl:
  region: Netherlands
  countries: ["NL"]
  auth: account
  directions: incoming+outgoing
  blurb: DHL eCommerce, tracked through your DHL account

ha-dpd:
  name: DPD
  region: Netherlands and 17 more countries — Germany on its own separate backend, the rest sharing DPD's myDPD network
  countries: ["NL", "AR", "BE", "HR", "CZ", "CH", "DE", "EE", "FR", "GB", "HU", "IT", "LV", "LT", "LU", "PT", "SK", "SI"]
  auth: account
  directions: incoming+outgoing
  blurb: DPD shipments, tracked through your myDPD account
  aliases:
    - name: BRT
      icon: brt.png
      region: Italy
      countries: ["IT"]
      blurb: BRT — DPD's brand name in Italy

ha-dragonfly:
  name: Dragonfly
  region: Netherlands & Australia
  countries: ["NL", "AU"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Dragonfly Shipping — no account and not even a postal code needed
  aliases:
    - name: Intelcom
      region: Canada
      countries: ["CA"]
      blurb: Intelcom — the same carrier group as Dragonfly Shipping, tracked under its Canadian brand name

ha-dynalogic:
  region: Netherlands & Belgium
  countries: ["NL", "BE"]
  auth: trackingnr
  input: order number + postal code
  directions: incoming
  blurb: Dynalogic last-mile deliveries, including those sold as MediaMarkt, Samsung, Nespresso and Menken

ha-econt:
  region: Bulgaria
  countries: ["BG"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Econt — Bulgaria's largest private courier, tracked without an account

ha-elta-courier:
  region: Greece
  countries: ["GR"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: ELTA Courier — the courier arm of Greece's national post, tracked without an account

ha-evri:
  region: United Kingdom
  countries: ["GB"]
  auth: trackingnr
  input: tracking code + postal code
  directions: incoming+outgoing
  blurb: Evri parcels and packages in the UK, tracked without an account

ha-fan-courier:
  region: Romania
  countries: ["RO"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: FAN Courier — Romania's parcel network, tracked without an account

ha-fedex:
  region: Worldwide
  countries: []
  auth: apikey
  input: tracking code
  directions: incoming
  blurb: FedEx parcels worldwide, through FedEx's official tracking API with a developer key you provide yourself

ha-gls:
  region: 20 countries — Austria, Belgium, Croatia, Czechia, Denmark, Finland, France, Hungary, Ireland, Italy, Luxembourg, Serbia, Slovakia, Slovenia and Switzerland share GLS's pan-EU tracker; Canada, Germany, the Netherlands, Poland and the United States use national backends
  countries: ["NL", "DE", "AT", "BE", "CH", "HR", "CZ", "DK", "FI", "FR", "HU", "IE", "IT", "LU", "RS", "SK", "SI", "CA", "US", "PL"]
  auth: trackingnr
  input: tracking code + postal code (US and PL need only the tracking code)
  directions: incoming
  blurb: GLS parcels, just like the gls-info.nl website

ha-gofo:
  region: United States, Canada, Italy, France, Spain and the Netherlands
  countries: ["US", "CA", "IT", "FR", "ES", "NL"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: GOFO Express (CIRRO Parcel in Europe) — cross-border e-commerce last-mile tracking

ha-helthjem:
  region: Norway
  countries: ["NO"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Helthjem — Norwegian home delivery

ha-hermes:
  region: Germany
  countries: ["DE"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Hermes Paket

ha-inpost:
  region: Poland, plus keyless tracking in Italy, Portugal and the United Kingdom
  countries: ["PL", "IT", "PT", "GB"]
  auth: account
  directions: incoming
  blurb: InPost Paczkomat lockers, tracked through your InPost account in Poland or by tracking code elsewhere — a parcel ready for pickup shows the locker's name

ha-laposte:
  name: La Poste
  region: France
  countries: ["FR"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: La Poste tracking for Colissimo and Chronopost parcels, without an account
  aliases:
    - name: Chronopost
      icon: chronopost.png
      blurb: Chronopost — La Poste's express brand, same tracking lookup
    - name: Colissimo
      icon: colissimo.png
      blurb: Colissimo — La Poste's parcel brand, same tracking lookup

ha-matkahuolto:
  region: Finland
  countries: ["FI"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Matkahuolto — Finnish parcel and logistics tracking, without an account

ha-nova-post:
  region: Ukraine, Europe and the US
  countries: ["UA", "MD", "PL", "LT", "CZ", "RO", "DE", "SK", "EE", "LV", "HU", "IT", "FI", "GB", "FR", "ES", "AT", "NL", "US"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Nova Post (Nova Poshta) — Ukraine's largest parcel-locker and courier network, with cross-border delivery across Europe and the US

ha-nz-post:
  region: New Zealand
  countries: ["NZ"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: New Zealand's national postal operator

ha-oesterreichische-post:
  region: Austria
  countries: ["AT"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Österreichische Post — Austria's national postal operator

ha-ontrac:
  region: United States
  countries: ["US"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: OnTrac — US regional parcel delivery across the West and Midwest

ha-orlen-paczka:
  region: Poland
  countries: ["PL"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: ORLEN Paczka — Poland's parcel locker network, tracked without an account

ha-speedx:
  region: United States
  countries: ["US"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: SpeedX package tracking without an account

ha-packeta:
  name: Packeta
  region: Central Europe, Germany & Slovenia
  countries: ["CZ", "SK", "HU", "PL", "RO", "DE", "SI"]
  auth: trackingnr
  input: tracking code
  directions: incoming+outgoing
  blurb: Packeta's Central-European pickup-point and locker network, known as Zásilkovna in Czechia
  aliases:
    - name: Zásilkovna
      region: Czechia
      countries: ["CZ"]
      blurb: Zásilkovna — Packeta's own brand name in Czechia, same backend and "Z" tracking codes

ha-planzer:
  region: Switzerland & Liechtenstein
  countries: ["CH", "LI"]
  auth: trackingnr
  input: tracking code or IKEA order number
  directions: incoming
  blurb: Planzer — the carrier behind IKEA Switzerland home deliveries

ha-poste-italiane:
  region: Italy
  countries: ["IT"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Poste Italiane — Italy's national postal operator, tracked without an account

ha-posten-bring:
  name: Posten
  icon: posten.png
  region: Norway, Sweden and Denmark
  countries: ["NO", "SE", "DK"]
  auth: account
  directions: incoming+outgoing
  blurb: Posten parcels, tracked through your own consumer account
  aliases:
    - name: Bring
      icon: bring.png
      blurb: Bring — Posten's sister brand, same backend and account model

ha-postnl:
  region: Netherlands & Belgium
  countries: ["NL", "BE"]
  auth: account
  directions: incoming+outgoing
  blurb: PostNL parcels plus announced MyMail letters

ha-postnord:
  region: Nordics
  countries: ["SE", "DK", "NO", "FI"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: PostNord — the Nordic postal operator

ha-ppl-cz:
  region: Czechia
  countries: ["CZ"]
  auth: account
  directions: incoming+outgoing
  blurb: PPL — one of Czechia's two dominant private carriers, passwordless e-mail + PIN login

ha-quickpac:
  region: Switzerland
  countries: ["CH"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Quickpac — the delivery arm for Galaxus/Digitec and other Swiss e-commerce

ha-sameday:
  region: South-East Europe
  countries: ["RO", "HU", "BG"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Sameday — the easybox locker network

ha-shopee-xpress:
  region: Brazil, Indonesia, Malaysia, the Philippines, Thailand & Vietnam
  countries: ["BR", "ID", "MY", "PH", "TH", "VN"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Shopee Xpress (SPX) — Shopee's own in-house last-mile carrier for its Southeast Asian and South American markets

ha-slovak-parcel-service:
  region: Slovakia
  countries: ["SK"]
  auth: trackingnr
  input: tracking code + postal code
  directions: incoming
  blurb: Slovak Parcel Service (SPS) — Slovakia's largest private parcel network, tracked without an account

ha-slovenska-posta:
  region: Slovakia
  countries: ["SK"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Slovenská Pošta parcels, tracked the same way as on posta.sk

ha-sunyou:
  region: Cross-border
  countries: []
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: SunYou (SYPost) — cross-border tracking for Asia-to-Europe e-commerce parcels, ahead of the local carrier that delivers them

ha-swiss-post:
  region: Switzerland
  countries: ["CH"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: Swiss Post — Switzerland's national postal operator

ha-trunkrs:
  region: Netherlands
  countries: ["NL"]
  auth: trackingnr
  input: tracking code + postal code
  directions: incoming
  blurb: Trunkrs — same-day and next-day delivery

ha-uniuni:
  region: Canada & United States
  countries: ["CA", "US"]
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: UniUni — e-commerce last-mile delivery across Canada and the US, tracked without an account

ha-ups:
  region: Worldwide
  countries: []
  auth: trackingnr
  input: tracking code
  directions: incoming
  blurb: UPS parcels worldwide, tracked without a UPS account — updates arrive slowly, because UPS answers a home connection only a few times an hour

ha-vinted-go:
  region: Netherlands, Belgium, France, Spain & Portugal
  countries: ["NL", "BE", "FR", "ES", "PT"]
  auth: account
  directions: incoming+outgoing
  blurb: Vinted Go (formerly Homerr) — passwordless e-mail login, locker and parcel-shop network
